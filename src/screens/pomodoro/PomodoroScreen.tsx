import { screenStyles } from '@/src/theme'
import { StyleSheet, Text, View } from 'react-native'

const PomodoroScreen = () => {
  return (
    <View style={screenStyles.container}>
        <Text>Pomodoro</Text>
    </View>
  )
}
const styles = StyleSheet.create({})
export default PomodoroScreen